# Fahrenheit-to-celsius
Project Description:  A simple Python temperature converter that takes a temperature in Fahrenheit as input and converts it into Celsius using the standard conversion formula. This project demonstrates basic Python concepts such as user input, variables, arithmetic operations, and type conversion.
